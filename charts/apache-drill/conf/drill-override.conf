{{- $count := int .Values.drill.count }}
drill.exec: {
  cluster-id: {{ .Values.drill.id | quote }},
  zk.connect: "{{- range $i, $e := until $count -}}
server.{{$i}}=drillcluster1-drillbit-{{$i}}.drill-service.apache-drill.svc.cluster.local:2181
{{- if lt $i (sub $count 1) -}}
,
{{- end }}
{{- end }}"
}